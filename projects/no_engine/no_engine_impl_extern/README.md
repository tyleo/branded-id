# No Engine Impl Extern

A implementation of the abstractions which can be passed across FFI boundaries. This can be
statically linked in to support multiple backends without needing to recompile the game.
